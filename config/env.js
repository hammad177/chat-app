import {
  AUTH_TOKEN_KEY as AUTH,
  ROOM_TOKEN_KEY as ROOM,
  BASE_URL,
} from "../constants";

export const SOCKET_URL = `${BASE_URL}/socket`;

export const SERVER_URL = `${BASE_URL}/api`;

export const AUTH_TOKEN_KEY = AUTH;

export const ROOM_TOKEN_KEY = ROOM;
